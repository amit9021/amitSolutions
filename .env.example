# Daily SEO Content Generation Configuration
# Copy this file to .env and fill in your values

# ============================================
# LLM Provider Configuration (Content Generation)
# ============================================

# LLM Provider (groq | openai)
# Default: groq (faster and cheaper)
LLM_PROVIDER=groq

# Groq API Key (REQUIRED if using Groq)
# Get your key from: https://console.groq.com/keys
GROQ_API_KEY=gsk_...

# OpenAI API Key (REQUIRED if using OpenAI)
# Get your key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-...

# LLM Model (optional - uses provider's default if not specified)
# Groq models: llama-3.3-70b-versatile (default), llama-3.1-70b-versatile, mixtral-8x7b-32768
# OpenAI models: gpt-4o-mini (default), gpt-4o, gpt-4-turbo
# LLM_MODEL=llama-3.3-70b-versatile

# ============================================
# Keyword Research Configuration
# ============================================

# Keyword Provider (stub | ahrefs | semrush)
# Default: stub (uses mock keywords, no API key needed)
KEYWORD_PROVIDER=stub

# Keyword Provider API Key (only needed if using ahrefs or semrush)
# KEYWORD_API_KEY=your-keyword-api-key-here

# Seed Queries (comma-separated)
# Optional: additional topics to generate content about
SEED_QUERIES=בניית אתרים,קידום אתרים,SEO,עיצוב אתרים

# ============================================
# General Configuration
# ============================================

# Base URL of your website (for sitemap)
BASE_URL=https://amit-solutions.co.il

# Number of posts to generate per day (default: 2)
POSTS_PER_DAY=2

# Draft Mode (true | false)
# If true, posts are marked as drafts for manual review
# Default: true
DRAFT_MODE=true
